import Foundation

/// Stable identifiers shared between the widget extension and the app
/// (which asks WidgetKit/Control Center to reload them).
public enum WidgetKinds {
    public static let statusWidget = "com.iblocker.widget.status"
    public static let protectionControl = "com.iblocker.control.protection"
}
